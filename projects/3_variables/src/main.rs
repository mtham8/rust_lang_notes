fn main() {
    /*
    let x = 5;
    let x = x + 1;
    let x = x * 2;
    println!("The value of x is: {}", x);
    */
    /*
    let spaces = "  ";
    let spaces = spaces.len();
    println!("{}", spaces);
    */
    let tup = (500, 6.4, 1);
    let (x, y, z) = tup;
    println!("The values are: {}, {}, {}", x, y, z);
}
