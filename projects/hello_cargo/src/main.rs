// new project: cargo new <project_name>
// build: cargo build
// exec: ./target/debug/hello_cargo
// build + exec = cargo run
// validate: cargo check
// release build: cargo build --release
fn main() {
    println!("Hello, world!");
}
